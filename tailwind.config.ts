// Tailwind v4 does not use this file for theme config.
// Theme tokens are defined in app/globals.css using @theme {}.
// This file is kept as a placeholder — safe to delete entirely.
import type { Config } from "tailwindcss";
const config: Config = { content: [] };
export default config;
